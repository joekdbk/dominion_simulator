class Card:
    def __init__(self, name, coin, actions, draws, vpoints, isAction):
        self.name = name
        self.coin = coin
        self.actions = actions
        self.draws = draws
        self.vpoints = vpoints
        self.isAction = isAction

    #def method1(self):
        # Method 1 implementation
     #   pass

# Instantiate objects
#obj1 = Card(param1_value, param2_value)
#obj2 = Card(param1_value, param2_value)

# Access object attributes and methods
#print(obj1.param1)
